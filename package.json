{
  "name": "tech-test-spectrum",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18 <=18.20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --parallel --filter \"./apps/*\" dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "format": "pnpm -r format"
  },
  "pnpm": {
    "overrides": {
      "fastify": "4.29.1"
    }
  }
}
